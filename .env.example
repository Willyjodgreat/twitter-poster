# ==================== TWITTER BOT CONFIGURATION ====================

# Server Configuration
API_PORT=3003
SSL_PORT=3443

# Security Settings
REQUIRE_API_KEY=true
API_KEYS=your-secret-api-key-here,another-key
ALLOWED_IPS=127.0.0.1

# Rate Limiting (modify with caution)
DAILY_LIMIT=500
HOURLY_LIMIT=60
MIN_DELAY=180000  # 3 minutes in milliseconds
MAX_DELAY=360000  # 6 minutes in milliseconds

# Proxy Configuration
USE_PROXY=false
PROXY_LIST=proxy1:port,proxy2:port

# Browser Configuration
USE_STEALTH=true
MAX_BROWSERS=3

# CORS (for web dashboard)
CORS_ORIGIN=*

# ==================== DEPLOYMENT NOTES ====================
# 1. Copy this file to .env
# 2. Replace values with your actual settings
# 3. NEVER commit .env to version control
